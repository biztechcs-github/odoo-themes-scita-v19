# ADR Preferences

preferred-style: madr-minimal

Rationale: matches the format already established by `adr/0001-devextreme-grid-remote-operations.md` (Status / Context / Decision / Consequences) and the OpenSpec `adr` artifact instruction, which mandates "MADR-short sections: Context, Decision, and Consequences" for every repo-level ADR. Set automatically during the `super-admin-holiday-approval-access` change's ADR review since a working precedent already existed; not asked interactively.
